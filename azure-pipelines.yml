# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- script: echo Building project
  displayName: 'step building project'

- script: echo Testing project
  displayName: 'step testing project'

- script: echo Publishing project
  displayName: 'step publish project'
  
- script: echo Deploying project
  displayName: 'step deploying project'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'
